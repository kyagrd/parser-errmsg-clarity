Here is the corrected code:

```
if (x > 0) {
  a = 1; // else b = 2; //
}
```

Note that in JavaScript, you cannot have multiple statements on the same line after an `if` statement. So, I've moved the assignment for `b` to a new line.

