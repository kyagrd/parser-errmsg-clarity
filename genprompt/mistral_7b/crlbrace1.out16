 Here's the corrected code:

```
if (x > 0) { a = 1; } else { b = 2; }
```

The issue was that the curly brace for the `else` clause was missing, causing the unexpected '}' error. By adding an opening curly brace after the `else` statement, we've resolved the syntax issue.

