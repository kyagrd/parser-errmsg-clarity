 Here's the corrected code:

```
if (x > 0) @a = 1; else @b = 2;
```

In your original code, there was a missing semicolon after the `else` statement, which caused the syntax error. The corrected version includes a semicolon to separate the two statements correctly.

