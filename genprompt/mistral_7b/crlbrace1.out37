 ```
if (x > 0) { a = 1; } else { b = 2; }
```

Here's how I fixed the error:

* Removed the extra '}' after `else` statement.
* Added curly braces for the `else` block to make it a proper block scope.

