 Here's the corrected code:

```
if (x > 0) { a = 1 } else { b = 2 };
```

The original code was missing curly braces {} and a semicolon ; after the first line, which were causing the syntax error. The corrected code includes these missing elements.

