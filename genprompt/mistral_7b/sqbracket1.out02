 Here is the corrected version of your code:

```
if (x > 0) [ a = 1; ] else [ b = 2; ];
```

I added braces around `b = 2` to fix the syntax error, which was caused by missing enclosing braces for the statement after the `else` clause.

