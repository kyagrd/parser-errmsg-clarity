 Here's the corrected syntax for your code:

```
if (x > 0) { a = 1; } else { b = 2; }
```

The issue was that the curly braces `{}` were missing, which enclose the statements that belong to the if-else block.

