 Here is the corrected version of your code:

```
if (x > 0) { a = 1; } else { b = 2; }
```

The issue was that the `else` statement was not properly indented and there was an extra closing brace. The corrected code now has the correct indentation and no extra braces.

