 Here's the corrected code:

```
if (x > 0)
  a = 1;
else
  b = 2;
```

In the original code, the issue was missing curly braces `{}` for the if-else block. By adding them, we have corrected the syntax error.

