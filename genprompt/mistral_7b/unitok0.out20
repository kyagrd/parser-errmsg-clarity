 Here's the corrected code with the syntax error fixed:

```
if (x > 0) { a = 1; } else { b = 2; }
```

I added curly braces `{}` to enclose the statements in each conditional block, which should help resolve the syntax issue.

