 Here is the corrected code:

```
if (x > 0) [ a = 1; ] else [ b = 2; ]
```

In the original code, there was an extra closing bracket `]` after the else clause, which caused the syntax error. I removed it and added another opening bracket before the assignment of `b = 2` to fix the issue.

