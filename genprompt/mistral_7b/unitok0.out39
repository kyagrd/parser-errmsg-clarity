 Here is the corrected code with the syntax error fixed:

```
if (x > 0) { a = 1; } else { b = 2; }
```

I added curly braces to enclose the statements within the `if` and `else` blocks, as required in C/C++.

