 Here is the corrected version of your code:

```
if (x > 0) [ a = 1; ] else [ b = 2; ];
```

I've added square brackets around `b = 2;` to match the pairing with `[ a = 1; ];`. This way, both conditional statements have corresponding closing brackets.

