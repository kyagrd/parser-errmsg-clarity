 ```
if (x > 0) [ a = 1; ] else b = 2
```

In the original code, the closing bracket `]` after the `else` clause is causing the error. To fix this, we simply remove it:

```
if (x > 0) [ a = 1; ] else b = 2
```

