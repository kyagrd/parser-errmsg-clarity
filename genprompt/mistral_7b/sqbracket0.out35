 Here is the corrected syntax for your code:

```
if (x > 0) { a = 1; } else { b = 2; }
```

In this corrected version, I added opening and closing curly braces to enclose the statements within each conditional branch. This should resolve the syntax error.

