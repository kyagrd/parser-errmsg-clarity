 Here is the corrected code with the syntax error fixed:

```
if (x > 0) [ a = 1; ] else [ b = 2; ];
```

In the original code, there was an extra closing bracket `]` after the `else` clause. I removed it and added another one to close the if-statement correctly.

