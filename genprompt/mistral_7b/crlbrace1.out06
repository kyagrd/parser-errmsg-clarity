 Here's the corrected version of your code:

```
if (x > 0) { a = 1; } else { b = 2; }
```

The issue was that you were missing the curly braces for the else clause, which should enclose the statement that gets executed when the condition in the if statement is false.

