 Here's the corrected version of your code:

```
if (x > 0) {
  a = 1;
} else {
  b = 2;
};
```

I added curly braces `{}` to enclose the statements within each conditional branch, and semicolons `;` after each statement for better readability. This should resolve the syntax error in your code.

