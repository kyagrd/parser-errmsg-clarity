 Here's the corrected version of your code, fixing the syntax error:

```python
if (x > 0) { a = 1; } else { b = 2; }
```

In your original code, you forgot to include curly braces for the `else` statement, which is causing the syntax error. The corrected version now includes them.

