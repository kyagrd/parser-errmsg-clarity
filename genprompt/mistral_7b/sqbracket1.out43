 Here is the corrected code with the syntax error fixed:

```
if (x > 0) [ a = 1; ] else b = 2
```

The issue in the original code was that there was an extra closing bracket `]` after the else clause. I removed it, and now the if-else statement is correctly formatted.

