 Here is the corrected version of your code:

```
if (x > 0) @ a = 1; else b = 2;
```

In your original code, you had multiple statements on the same line for an `if` condition, which is not allowed in this context. I combined the conditions into a single statement.

