 Here is the corrected version of your code:

```
if (x > 0) { a = 1; } else { b = 2; }
```

In your original code, you forgot to enclose the `else` statement in curly braces. This fixed version now includes those braces.

