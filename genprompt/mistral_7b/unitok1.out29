 Here's the corrected code:

```
if (x > 0) { a = 1 } else { b = 2 };
```

By enclosing the `if` and `else` statements in curly braces and using semicolons to separate each statement, we fix the syntax error.

