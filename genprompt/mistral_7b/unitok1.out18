 ```
if (x > 0) { a = 1; } else { b = 2; }
```
In the corrected code, I replaced the incorrect syntax for if-else statements with proper curly braces to enclose the blocks of code that should execute depending on the condition.

