 Here's the corrected code:

```
if (x > 0) { a = 1; } else { b = 2; }
```

I added curly braces `{}` to enclose the statements within each conditional branch, making it clearer and fixing the syntax error.

