---
errmsg: syntax error
---
code: |
  if (x > 0) { a = 1; c = 3; } else b = 2; }
---
more: |
  Note the syntax of the code above may be DIFFERENT from exsting languages,
  such as C, Java, Python, etc. Try to fix the error using symbols that
  appear in the error message, if available.
  DO NOT output your "thinking" process. ONLY output wat was asked.
---
