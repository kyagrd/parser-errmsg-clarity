Thinking...
We need to fix syntax error in code:

```
if (x > 0) 《 a = 1; 》 else b = 2; 》
```

The problem: using Chinese brackets 《 》 instead of parentheses or braces. Also missing braces. The proper syntax: `if (x > 0) { a = 1; } else { b = 2; }`

Thus output only fixed code inside triple backticks.

...done thinking.

```c
if (x > 0) { a = 1; } else { b = 2; }
```


