Thinking...
We need to fix syntax error using only error message. But we can deduce typical syntax: if statement should be: if (x > 0) { a = 1; } else { b = 2; }. Provided code has mismatched brackets and parentheses. The error message is "syntax error". We fix it. Provide only code inside triple backticks.

Let's output:

```
if (x > 0) { a = 1; } else { b = 2; }
```
...done thinking.

```
if (x > 0) { a = 1; } else { b = 2; }
```



